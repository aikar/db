language: java
dist: trusty
jdk:
  - oraclejdk8
git:
  submodules: false
before_install:
  - git config --global user.email "travis-ci@travis-ci.com"
  - git config --global user.name "Travis CI"
  - ./gradlew build
cache:
  directories:
    - '$HOME/.gradle'
